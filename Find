def largestAltitude(self, gain):
        """
        :type gain: List[int]
        :rtype: int
        """
        res=[0,]
        count=0
        for i in range(len(gain)):
            count=count+gain[i]
            res.append(count)
        return max(res)
